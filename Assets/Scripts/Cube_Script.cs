using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cube_Script : MonoBehaviour
{
    /*public float speed = 20;
    private int countSpace = 0;
    private BoxCollider boxCollider;
    // Start is called before the first frame update
    void Start()
    {
        boxCollider = GetComponent<BoxCollider>(); //recupération d'un composant 
    }

    // Update is called once per frame
    void Update()
    {
        float horizontal = Input.GetAxis("Horizontal");
        //Debug.Log(horizontal);
        float vertical = Input.GetAxis("Vertical");
        //Debug.Log(vertical);
        transform.Translate(new Vector3(horizontal, 0, vertical) * Time.deltaTime * speed);
        //Debug.Log(boxCollider.center);

    }

    void OnTriggerEnter (Collider collider)
    {
        if(collider.CompareTag("Zone")) {
            Debug.Log("Zone Infiltrated !!!")
;        }
    }*/
}
